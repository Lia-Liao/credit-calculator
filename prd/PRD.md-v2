# 信用卡管理后台 - 设计方案 v2

## 📋 需求分析

### 改进点：

1. ✅ **简化顶部区域**
   - 删除"添加新信用卡"表单板块
   - 只保留一个"➕ 添加信用卡"按钮
   - 点击按钮后在下方列表新增项目

2. ✅ **合并金额字段**
   - 删除独立的"账单出账金额"字段
   - 删除独立的"现金授信额度"字段
   - **账单分期**: 设置一个确定的"账单出账金额"（不是范围）
   - **现金分期**: 设置金额范围，最低默认 1000，最高为授信额度

3. ✅ **增加产品修改功能**
   - 现有分期产品支持编辑
   - 可修改金额（账单出账金额 / 现金分期金额范围）
   - 可修改期数和利率配置

4. ✅ **收缩式添加产品**
   - "添加分期产品"默认只显示一行
   - 点击后展开完整表单
   - 减少视觉干扰

---

## 🎨 界面设计草图

```
┌─────────────────────────────────────────────────────────────────────┐
│  🏦 信用卡管理后台                                    [➕ 添加信用卡] │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  招商银行                                    [删除] [编辑] │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │                                                             │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  [账单分期]  账单出账金额: ¥15000  [编辑]        │   │   │
│  │  ├─────────────────────────────────────────────────────┤   │   │
│  │  │  3期 → 4.50%  │  6期 → 4.20%  │  12期 → 4.80% │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  [现金分期]  金额范围: ¥1000 - ¥50000  [编辑]    │   │   │
│  │  ├─────────────────────────────────────────────────────┤   │   │
│  │  │  3期 → 5.20%  │  6期 → 4.90%  │  12期 → 5.50% │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  [➕ 添加分期产品] 点击展开                        │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │     (点击后展开为完整表单)                                │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  工商银行                                    [删除] [编辑] │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │  ... (同上结构) ...                                        │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📐 详细设计

### 1. 顶部操作区

```
[🏦 信用卡管理后台]                    [➕ 添加信用卡]
```

- 左侧：标题
- 右侧：单个"添加信用卡"按钮
- 点击按钮 → 在列表末尾添加新的信用卡卡片

---

### 2. 信用卡卡片结构

每张信用卡卡片包含：

```
┌─────────────────────────────────────────────────────────┐
│  [银行名称]                              [删除] [编辑] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [分期产品分组1 - 账单分期]              [编辑]        │
│  账单出账金额: ¥15000                                  │
│  [3期 → 4.50%] [6期 → 4.20%] [12期 → 4.80%]       │
│                                                         │
│  [分期产品分组2 - 现金分期]              [编辑]        │
│  金额范围: ¥1000 - ¥50000                              │
│  [3期 → 5.20%] [6期 → 4.90%] [12期 → 5.50%]       │
│                                                         │
│  [➕ 添加分期产品]  (收缩状态，点击展开)                │
└─────────────────────────────────────────────────────────┘
```

---

### 3. 添加分期产品（收缩状态）

**默认状态（收缩）：**
```
[➕ 添加分期产品]  点击展开配置
```

**点击后展开：**
```
┌─────────────────────────────────────────────────────────┐
│ 产品类型: [账单分期 ▼]                                  │
│                                                         │
│ (如果是账单分期)                                        │
│ 账单出账金额: [15000]                                  │
│                                                         │
│ (如果是现金分期)                                        │
│ 最低借款金额: [1000]                                   │
│ 最高借款金额: [50000]                                  │
│                                                         │
│ 期数和利率:                                            │
│  [3期] [0.045]  [删除]                                │
│  [6期] [0.042]  [删除]                                │
│  [➕ 添加更多期数]                                      │
│                                                         │
│ [取消]  [添加产品]                                     │
└─────────────────────────────────────────────────────────┘
```

---

### 4. 编辑分期产品弹窗/表单

点击产品的"编辑"按钮后：

```
┌─────────────────────────────────────────────────────────┐
│ 编辑 [账单分期]                                          │
├─────────────────────────────────────────────────────────┤
│ 账单出账金额: [15000]                                  │
│                                                         │
│ 期数和利率配置:                                        │
│  ┌─────────────────────────────────────────────┐      │
│  │ 期数: [3]   利率: [0.045]  [删除]        │      │
│  ├─────────────────────────────────────────────┤      │
│  │ 期数: [6]   利率: [0.042]  [删除]        │      │
│  ├─────────────────────────────────────────────┤      │
│  │ 期数: [12]  利率: [0.048]  [删除]        │      │
│  └─────────────────────────────────────────────┘      │
│  [➕ 添加更多期数]                                      │
│                                                         │
│ [取消]  [保存修改]                                     │
└─────────────────────────────────────────────────────────┘

或者（如果是现金分期）：

┌─────────────────────────────────────────────────────────┐
│ 编辑 [现金分期]                                          │
├─────────────────────────────────────────────────────────┤
│ 最低借款金额: [1000]                                   │
│ 最高借款金额: [50000]                                  │
│                                                         │
│ 期数和利率配置:                                        │
│  ... (同上) ...                                        │
│                                                         │
│ [取消]  [保存修改]                                     │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 数据结构变化

### 之前：
```json
{
  "bankName": "招商银行",
  "billAmount": 15000,      // ← 删除
  "creditLimit": 50000,     // ← 删除
  "products": [...]
}
```

### 现在：
```json
{
  "bankName": "招商银行",
  "products": [
    {
      "productType": "bill",
      "billAmount": 15000,   // ← 账单出账金额（确定值，不是范围）
      "terms": [...]          // 期数和利率列表
    },
    {
      "productType": "cash",
      "minAmount": 1000,     // ← 最低借款金额（默认1000）
      "maxAmount": 50000,     // ← 最高借款金额（授信额度）
      "terms": [...]          // 期数和利率列表
    }
  ]
}
```

**注意**：实际产品数据保持原结构，每个期数单独一条记录，方便计算：
```json
{
  "id": 1,
  "productType": "bill",
  "billAmount": 15000,
  "term": 3,
  "rate": 0.045,
  "enabled": true
}

或者（现金分期）：
{
  "id": 4,
  "productType": "cash",
  "minAmount": 1000,
  "maxAmount": 50000,
  "term": 3,
  "rate": 0.052,
  "enabled": true
}
```

---

## ✅ 功能清单

- [x] 顶部只保留"添加信用卡"按钮
- [x] 删除独立的账单/授信金额字段
- [x] **账单分期**: 设置确定的"账单出账金额"（不是范围）
- [x] **现金分期**: 设置金额范围，最低默认1000，最高为授信额度
- [x] 分期产品支持编辑（账单金额/现金分期金额范围、期数、利率）
- [x] 添加产品做成收缩式
- [x] 所有代码同步修改
