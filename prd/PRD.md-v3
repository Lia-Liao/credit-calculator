# 信用卡借款计算器 - 前端UI优化方案 v3

## 📋 需求分析

### 1. 信用卡列表 Tab 页优化
在"刷新数据"按钮下方、信用卡列表上方，新增两个统计信息：
- 当前总负债 = 所有银行出账金额相加
- 当前融资能力 = 所有银行现金授信额度相加
  - 右侧显示"i"信息提示图标
  - 悬停时显示：银行可能动态调整您的授信额度，实际额度以银行办理页为准

### 2. 计算最优 Tab 页布局调整
- **借款金额区域**：
  - 文字：借款金额
  - 输入框
  - 下方小字提示：一键填入当前总负债
- **借款时长区域**：
  - 文字：借款时长
  - 最小期数选择框
  - 最大期数选择框

---

## 🎨 页面设计草图

### 完整页面布局

```
┌─────────────────────────────────────────────────────────────────┐
│                    信用卡借款计算器                              │
├─────────────────────────────────────────────────────────────────┤
│  [信用卡列表]  [计算最优]                                       │
├─────────────────────────────────────────────────────────────────┤
│  信用卡列表 Tab:                                                │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  [刷新数据]                                              │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  当前总负债：¥40,000.00  ┊  当前融资能力：¥130,000.00 [i] │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  [招商银行卡片]                                           │  │
│  └─────────────────────────────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  [工商银行卡片]                                           │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 信用卡列表 Tab 详细设计

```
信用卡列表 Tab:
│
│  ┌─────────────────────────────────────────────────────────┐
│  │  [刷新数据]                                              │
│  ├─────────────────────────────────────────────────────────┤
│  │  当前总负债：¥40,000.00  ┊  当前融资能力：¥130,000.00 ⓘ  │
│  │                                                      ↑  │
│  │                                        ┌───────────────┐│
│  │                                        │ 银行可能动态   ││
│  │                                        │ 调整您的授信   ││
│  │                                        │ 额度，实际额   ││
│  │                                        │ 度以银行办理   ││
│  │                                        │ 页为准         ││
│  │                                        └───────────────┘│
│  └─────────────────────────────────────────────────────────┘
│
│  [信用卡列表...]
```

### 计算最优 Tab 布局

```
计算最优 Tab:
│
│  ┌─────────────────────────────────────────────────────────┐
│  │  借款金额                                               │
│  │  [ _________________________ ]                          │
│  │  一键填入当前总负债                                      │
│  ├─────────────────────────────────────────────────────────┤
│  │  借款时长  [3期 ▼]  ─ [24期 ▼]                        │
│  ├─────────────────────────────────────────────────────────┤
│  │  [计算最优方案]                                         │
│  └─────────────────────────────────────────────────────────┘
│
│  [计算结果显示区域...]
```

---

## 📝 详细设计说明

### 1. 统计信息区域

**位置**：刷新数据按钮下方，信用卡列表上方

**样式**：
- 卡片式容器
- 左右两个数据项
- 中间用分隔线隔开

**当前总负债**：
- 计算方式：所有信用卡的账单分期 `billAmount` 相加
- 显示格式：¥XX,XXX.XX
- 字体加粗突出显示

**当前融资能力**：
- 计算方式：所有信用卡的现金分期 `maxAmount` 相加
- 显示格式：¥XX,XXX.XX
- 字体加粗突出显示
- 右侧显示信息提示图标 ⓘ
  - 悬停时显示 tooltip 提示

**提示内容**：
```
银行可能动态调整您的授信额度，实际额度以银行办理页为准
```

---

### 2. 计算最优 Tab 布局

**借款金额区域**：
- 上一行：标签"借款金额" + 输入框
- 下一行：小字体文字"一键填入当前总负债"（链接样式）

**借款时长区域**：
- 标签"借款时长"
- 最小期数选择框
- 连接符号"—"
- 最大期数选择框

---

## 🎯 交互说明

1. **一键填入按钮**：
   - 点击时将"当前总负债"数值填入借款金额输入框
   - 如果没有账单金额则显示 0 或提示

2. **信息提示图标**：
   - 鼠标悬停显示 tooltip
   - 鼠标移开隐藏

---

## 📊 数据计算

**当前总负债 = sum(所有信用卡中 bill 类型产品的 billAmount)**

**当前融资能力 = sum(所有信用卡中 cash 类型产品的 maxAmount)**
